#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>

namespace msg_processing
{
    // struct Message;
    uint64_t calculateTimeMs(const uint8_t h = 0, uint8_t m = 0, uint8_t s = 0);
}

// struct msg_processing::Message
// {
//     float value;
//     bool is_valid;
// };


#endif  //UTILS_HPP